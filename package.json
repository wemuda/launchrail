{
  "name": "launchrail-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Launchrail — an updatable development system connecting vision, design, specs, tickets, bounded autonomous implementation, and verification.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wemuda/launchrail.git"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present test"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.6.5"
}
