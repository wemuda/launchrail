{
  "name": "@wemuda/launchrail",
  "version": "1.11.0",
  "description": "Launchrail — initialize, inspect, update, and validate repositories using the Launchrail development system.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "launchrail": "./dist/index.js"
  },
  "files": [
    "dist",
    "assets"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "prepack": "node scripts/generate-readme.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wemuda/launchrail.git",
    "directory": "packages/cli"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "yaml": "^2.9.0"
  }
}
