@AGENTS.md

## Project-specific Claude Code instructions

- This repo builds the tooling that other repos consume. When designing any feature that writes files into a consuming project, start from the ownership model (managed / seeded / project-owned) in AGENTS.md.
- The default branch is `master`.
